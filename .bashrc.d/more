# more aliases and exports 

# if less is installed then lets not use more
# aliase to more with color support
if [ -f $( which less ) ];
then
    echo "Using less in place of 'more'"
    alias more='less -R';
fi 
